/** Review annotation (requirement IDs). Set VITE_SHOW_SPEC_REFS=true only for design/spec review — never in production. */
export const SHOW_SPEC_REFS = import.meta.env.VITE_SHOW_SPEC_REFS === 'true';
export const CURRENCY = '₱';
